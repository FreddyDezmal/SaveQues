# dependencies
/node_modules
/.pnp
.pnp.js

# Sprint 18: Playwright artifacts and auth state — the latter contains
# real session cookies from a test account and must never be committed.
/e2e/.auth
/test-results
/playwright-report
/coverage

# Sprint 16: generated build metadata (regenerated by scripts/generate-build-info.js
# on every `pnpm run build` via the prebuild hook) — not source, never commit.
/lib/buildInfo.generated.json

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Sentry Config File
.env.sentry-build-plugin
